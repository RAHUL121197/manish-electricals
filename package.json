{
  "name": "manish-electricals",
  "version": "1.0.0",
  "private": true,
  "description": "MANISH ELECTRICALS - Company Website, Admin & Employee Management System",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "db:migrate": "node server/src/config/migrate.js",
    "db:seed": "node database/seed/seed.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.0",
    "pg": "^8.17.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
